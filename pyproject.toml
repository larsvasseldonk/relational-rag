[project]
name = "ai-buildcamp-project"
version = "0.1.0"
description = "My AI Engineering Buildcamp project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.0.0",
    "minsearch>=0.0.5",
    "openai>=1.50.0",
    "python-dotenv>=1.0.0",
]
